# 项目：在美华人第三国签证申请助手

## AI 开始工作前的必读顺序

任何 AI、自动化工具或新聊天线程在分析、规划、修改源码前，必须依次完整阅读：

1. `AGENTS.md`
2. `UPDATE.md`
3. `docs/PROJECT_CORE.md`
4. 与当前任务直接相关的其他 `docs/*.md`

每次修改源码、配置、产品文档或公开内容后，都必须在 `UPDATE.md` 顶部追加一条带有完整时间戳的记录。记录至少包括修改范围、关键决定和验证结果。

如果 `UPDATE.md` 不存在，必须先创建它，再修改其他文件。

## 这是什么

面向持中国护照、居住在美国的非移民身份用户，提供加拿大、日本、韩国等第三国签证的配置驱动、逐步申请流程。

产品不是传统攻略文章站。用户先确认美国身份，系统加载对应的流程配置，再通过 Checklist、官方链接和条件步骤，从零开始引导到收到签证。

个人项目，作品集性质，免费，无商业化。

0.1 版本只实现“中国护照 + 年满 18 岁 + 美国 F-1 在读学生 + 加拿大访客签证”流程，但底层必须保留扩展到 OPT、H、J、M、L 等身份以及其他国家的能力。

## 目标用户绝对不是

- 在中国大陆境内出境的用户
- 美国公民
- 美国永久居民（当前版本暂不覆盖）

任何内容、措辞、举例都要从“在美华人”视角出发。不要写“国内办理需要……”这类错位文案。

## 技术栈

- Next.js 14（App Router）
- TypeScript 严格模式
- Tailwind CSS
- MDX 用于解释性长内容
- 部署到 Vercel
- 暂不上数据库；结构化流程使用本地类型化配置

## 产品与数据结构

- 流程、步骤、Checklist、官方链接和时间线节点使用类型化配置。
- 身份差异通过独立 config 表达，不在页面组件内堆叠身份判断。
- MDX 只用于较长的解释性内容，不作为核心流程引擎。
- 0.1 使用浏览器本地存储保存非敏感进度。
- 不保存护照号、UCI、申请号、出生日期、银行资料、学校名称或用户上传文件。
- 易变化的费用、地址、时限和官方入口必须带最近核验日期。

## 写代码时的规则

- 每个组件单文件，Props 必须 TypeScript 类型化。
- 不要安装新依赖，除非绝对必要；必须先在计划里说明原因。
- 不要在没有询问用户前修改 `package.json` 的依赖版本。
- 所有用户可见文案默认中文，预留 i18n 结构但不立即实现。
- 移动端优先：所有页面先在 375px 宽度调通，再考虑桌面。
- React 组件只负责展示和交互；流程规则放在配置和 `lib` 中。
- 所有外部官方链接必须标明机构，并安全地在新标签页打开。
- 修改后至少运行与改动风险相匹配的类型检查、lint 或生产构建。

## 内容写作规则

- 所有关键申请要求必须引用官方来源。
- 站内流程正文对社区与博客信息统一以「第三方经验」「第三方报道」定性标注；第三方主要来源的名称与网址集中在「关于本站」的来源说明区介绍并链接引流，不在任务正文内逐条散布；引用的具体原文 URL 记录在内部文档（`UPDATE.md` 或 `docs/`）供核验。
- 不直接搬运他人内容，必须用自己的话重写。
- 不要把第三方经验写成官方规则。
- 不要未经官方核验硬编码可能变化的费用、VAC 地址或处理时长。
- 真实 DP 单独标注：日期、州、身份、结果和时间线。

## 绝对不要做

- 不要在用户 review 前 push 到 `main`。
- 不要安装 ESLint/Prettier 之外的 lint 工具。
- 不要 commit 任何 `.env` 文件。
- 不要把多个文件的改动塞进一个 commit；确实耦合时除外。
- 不要收集或存储用户的敏感签证申请信息。
- 不要把本站流程完成度描述为官方审理进度。

## Git 习惯

- 每个 commit 只动一个文件，除非文件确实耦合。
- Commit message 使用中文，格式为“动词: 简短描述”。
- 示例：“新增: 加拿大 F1 流程配置”。

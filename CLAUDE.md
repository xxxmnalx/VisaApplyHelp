# 项目：在美华人第三国签证攻略站

## 这是什么
面向"持中国护照、居住在美国"的用户群体（F1/OPT/H1B/H4/J1/绿卡），
提供加拿大/日本/韩国等第三国签证的结构化攻略。
个人项目，作品集性质，免费，无商业化。

## 目标用户绝对不是
- 在中国大陆境内出境的用户
- 美国公民

任何内容、措辞、举例都要从"在美华人"视角出发。
不要写"国内办理需要..."这种话。

## 技术栈
- Next.js 14 (App Router)
- TypeScript（严格模式）
- Tailwind CSS
- MDX 用于攻略内容
- 部署到 Vercel
- 不上数据库；结构化数据用 data/*.json

## 内容数据结构
每篇攻略是一个 .mdx 文件，frontmatter 必须包含：
- country: 国家代码（CA/JP/KR）
- visaType: 签证类型
- forStatus: 适配身份数组（如 ["F1", "H1B"]）
- lastUpdated: ISO 日期
- officialFee: 官费
- processingDays: 大致工作日

## 写代码时的规则
- 每个组件单文件、Props 必须 TypeScript 类型化
- 不要装新依赖，除非绝对必要——先在计划里说明为什么需要
- 所有用户可见文案默认中文，预留 i18n 结构（不立刻实现）
- 移动端优先：所有页面先在 375px 宽度调通，再考虑桌面

## 内容写作规则
- 攻略内容必须引用官方来源（脚注链接到使领馆官网）
- 引用其他博客（如北美票帝）时必须在文末"参考资料"列出原文 URL
- 不直接搬运他人内容，必须用自己的话重写
- 真实 DP（Data Point）单独标注：日期 + 州 + 身份 + 结果 + 时间线

## 绝对不要做
- 不要 push 到 main 分支前不让我 review
- 不要装 ESLint/Prettier 之外的 lint 工具
- 不要在没有问我之前修改 package.json 的依赖版本
- 不要 commit 任何 .env 文件
- 不要把多个文件的改动塞进一个 commit——一个文件一个 commit

## Git 习惯
- 每个 commit 只动一个文件（除非真的耦合）
- Commit message 用中文，格式："动词: 简短描述"
- 例："新增: 加拿大攻略 F1 身份页面骨架"
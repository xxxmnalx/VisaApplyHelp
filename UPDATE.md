# 项目修改记录

> 所有 AI、自动化工具和新聊天线程在修改源码前必须先阅读本文件。  
> 新记录按时间倒序添加，时间使用项目本地时区并包含时区缩写。

## 2026-07-09 16:46:33 PDT

### 修改范围（第三方来源展示策略：正文统一定性 + 关于页集中引流）

- **流程正文**（三国配置）：所有「第三方经验（北美票帝）」前缀统一改为「第三方经验：」；任务级第三方来源链接条目全部移除（ca：piaodi-ca / piaodi-asc-walkin；jp：piaodi-jp-evisa / piaodi-jp-consolidated；kr：piaodi-kr / piaodi-kr-transit / newsis-multi），相关 sourceIds 引用同步清理；「出发前看票帝实时反馈页」改写为「出发前多看近期社区反馈」。官方来源链接不受影响。
- **关于页**新增「第三方经验的来源」区：说明「第三方经验」标注的内容来自华人社区多年相互验证的公开分享（本站重写并定性、不代表官方规则），集中列出主要来源并做引流链接——北美票帝 piao.tips（系统化在美办签指南）与一亩三分地 1point3acres.com（社区真实 DP），均安全新标签页打开。
- **AGENTS.md 内容规则更新**：站内流程正文对社区与博客信息统一「第三方经验 / 第三方报道」定性、不在任务正文逐条散布站点名称与链接；第三方主要来源集中在关于页说明区介绍并引流；引用的具体原文 URL 记录在内部文档（UPDATE.md / docs）供核验。

### 关键决定

- 用户初始要求全站不出现第三方名称与网址，随后修正为：正文保持「第三方经验」统称，但在站内设集中说明区给来源引流（用户将联系第三方谈经验共享与互相推广）；本轮按修正后口径执行。
- 具体引用页的原文 URL（票帝加拿大指南、ASC walk-in 反馈页、日韩指南、Newsis 报道等）不再进入站内展示，但均已留存于本文件历史记录中，核验链条不断。
- 引流链接指向两家主站首页而非具体文章，展示位选在「关于本站」（与「我们会做什么 / 不会做什么」同层级），不干扰流程页的官方来源区。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过。
- 走查（dev server）：CA prepare、JP overview、KR consulate（原第三方引用最密集的三页）正文均无「票帝 / piao.tips / 一亩三分地 / Newsis」字样、保留「第三方经验」定性；关于页新区块两条引流链接均为新标签页打开。

## 2026-07-09 16:34:30 PDT

### 修改范围（CA 流程吸收北美票帝社区经验，操作细节全面加密度）

- 抓取票帝《在美国申请加拿大签证指南》全文（2026-07-09），按「第三方经验」定性写入 CA 流程 8 处，全部附原文来源链接：
  - **IMM 5257 填表坑**（prepare / imm5257）：UCI 号（如有）在过往加签签证页右下角 Person 下方黑体数字；在美换发护照 Country of issue 填中国、Place of Issue 填换发城市；学生 Employment 写在读身份 + 学校名。
  - **在线入口**（apply / official-entry）：持有效美签者多走 Portal 简化路径（先获取邀请码再注册，全程系统内答题，与官方 Portal 说明页交叉印证）；GCKey 路径 reference code 约 60 天有效、密保问题务必记牢。
  - **补充材料**（prepare / extra-documents）：常见被要求中国居民身份证正反面、旅行计划说明（订单非必需）。
  - **上传技巧**（submit / details-match）：单文件超限压到约 96dpi 通常可过。
  - **缴费**（submit / fees-paid）：支持主流信用卡含银联；2023-04 起申请费仅在线支付。
  - **采集预约**（biometrics / appointment-booked）：ASC 预约通常只放约两周后的号、收到 BIL 当天就约；部分 VAC 也可采集；walk-in 政策多变，新增票帝实时反馈页来源（piaodi-asc-walkin）。
  - **递照细节**（decision / vfs-submit）：OPR 即 IMM 5740；转运费 money order / cashier's check、抬头 VFS Services (USA) Inc.、不收个人支票（金额不硬编码，以 VFS 当前页为准）；VAC 录指纹同点递交可免转运费；多人合寄但回邮 label 一人一份。
  - **等待期心态**（decision / monitor-status）：顺利案例全程约 2–3 周，各阶段 1 天到 1 个月波动均属正常。
- decision 步死线 note 补「（第三方经验：通常约 30 天）」，chip 维持「限期寄达」（官方未核验固定天数，以通知标注日期为准）。

### 关键决定

- 用户指出官方站反爬时应转向票帝 / 一亩三分地等多年社区验证的第三方源：采纳。票帝指南可直接抓取且系统化，作为本轮主源；一亩三分地正文在登录墙后、仅能取到片段预览，本轮不作为引用来源（后续可人工收集 DP）。
- 所有社区来源信息一律「第三方经验（北美票帝）」前缀定性 + sourceIds 关联原文，不与官方要求混写；易变项（VAC 地址、转运费金额）仍不硬编码，只写相对稳定的操作要领（抬头、支付形式、免转运费条件）并指向 VFS 当前页。
- Portal 简化路径的存在同时经官方 portal-application-process 页与票帝交叉印证后才写入。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过。
- 走查（dev server）：biometrics 步「预约采集点」展开可见两条票帝经验 + walk-in 反馈页链接（与 IRCC 官方来源并列、定性清晰）；「后程期限」卡第 6 步 note 显示 PPR / OPR 与第三方经验口径。

## 2026-07-09 16:24:53 PDT

### 修改范围（CA 流程 IMM 5257 教学内容 + 里程碑条错位修复）

- **IMM 5257 主申请表写入加拿大流程**（用户反馈：网站没提示这一关键信息、第 3 步只给链接不教学）。新增官方来源三条（均 2026-07-09 核验）：IMM 5257 表格官方下载页、「如何填写并验证带条码的申请表」官方 FAQ（qnum=1523）、「什么是个性化材料清单」官方 FAQ（qnum=829）。
- **「准备材料」步**新增任务「认识主申请表 IMM 5257」（视情况）：展开要点覆盖 官方下载入口（勿用第三方模板）→ 必须下载后用 Adobe Acrobat Reader ≥10 打开（浏览器 / 预览打不开也验证不了）→ 填写要求 → 点「Validate」生成条码页 → 在线保存 / 纸质条码页置顶；并注明现行 2023-09 版已并入背景声明、无单独 Schedule 1。
- **「在线填写」步改为指路式**：`official-entry` 任务展开写明两条在线路径（A · IRCC Portal 系统内直接答题、不需要 IMM 5257 PDF；B · IRCC secure account / GCKey 问卷 → 个性化清单 → 上传验证过条码的 IMM 5257）与注册要点；`personal-checklist` 任务补官方 FAQ 定义与用法；新增条件任务「（GCKey 路径）验证并上传 IMM 5257」（Validate 失败排查、上传报错第三方经验单独定性）；步骤主按钮入口改为「访客签证申请步骤」页。CA 流程 `lastVerified` 提升至 2026-07-09。
- **里程碑条错位修复**（用户反馈：到当前节点时圆点和线段下移几个像素）：连接线行固定 30px 高，26px 普通节点与 30px 当前节点共用同一中轴；并按设计稿 1b 补回当前节点下方的「当前」小标（增强当前态辨识）。

### 关键决定

- canada.ca 与 ircc.canada.ca 对抓取工具返回 403，事实核验改走多源检索交叉（官方 FAQ 两条 + 表格页 + 多家第三方一致），与 0.3 处理日本官网反爬的先例一致；「上传报错常见原因」定性为第三方经验，不写成官方规则。
- IRCC 在线入口存在 Portal 与 secure account 双路径且偶有调整：教学内容写「两条路径的分野与各自动作」，每一小步锚定官方页面链接，不逐屏截图硬编码官方系统 UI（易变、无法核验）。
- 任务在 0.3 版本内新增（`imm5257`、`upload-imm5257`），不改流程版本号：`normalizeProgress` 对新任务默认待办，旧进度不受影响。
- 日韩流程与 CA 其余步骤的同等深度教学内容留作后续批次（需逐国官方核验）。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过。
- 走查（dev server，1280）：第 4 步页面全部连接线段 top 同为一个值、7 个圆点圆心同轴（几何测量 segmentTops 仅 1 个值），「当前」徽标正常；第 2 步 IMM 5257 任务展开五条要点 + 两条官方来源链接；第 3 步任务读作「入口 → 双路径 → 类别确认 → 问卷出清单 → 验证上传 IMM 5257 → 如实填写」，主按钮指向「访客签证申请步骤」官方页。

## 2026-07-09 16:15:30 PDT

### 修改范围（国家卡预计拿签日期 + 费用分项与美元换算）

- **国家卡「预计拿到」日期**（用户反馈）：`CountrySelector` 在「平均约 X」之后新增一行「现在开始，预计 yyyy 年 m 月 d 日前拿到（估算）」——按今天为提交日、复用 `estimateEta` 叠加各阶段最长天数得出，日期等宽显示；仅未开始的卡显示（已有进度的卡在中途，整程估算不适用，仍显示断点续接条）。
- **费用分项 + 美元换算**（用户反馈）：`FlowConfig.feeItems` 新增结构化费用分项（label / amount / currency / approximate / note），费用面板（`FlowOverviewPanel`）渲染「分项 → 总计 → 按当前汇率约合 USD」表格；官方口径为「约」的金额带「约」前缀并传染到总计。三国配置写入：CA = 申请费 CAD 100 + 生物信息费 CAD 85（家庭上限与 10 年免采集写入 note）；JP = eVISA 约 JPY 15,000（`approximate`，纸质路径 USD 101/201 写入 note）；KR = 单次 USD 40（两次/多次/长期与 money order 口径写入 note）。金额均为既有已核验值，未新增未核验数字。
- **汇率来源**：新增 `lib/exchange-rate.ts`，客户端调 Frankfurter API（欧洲央行参考价、无密钥、不发送任何用户数据）换算兑 USD；UI 标注「1 CCY ≈ rate USD，欧洲央行参考价 + 数据日期」；本币即 USD（韩国）不显示换算行；请求失败静默降级为只显示本币总计。免责句改为「费用与汇率为参考，实际扣款金额以官方支付页面为准」。
- 无结构化分项的流程保留原文字口径展示路径（向后兼容）。

### 关键决定

- 「预计 X 日前拿到”取阶段最长天数之和（而非平均），并保留「（估算）」后缀与等宽日期——比「平均约 X 周」直观，同时不违反「估算非保证、非上限」底线（PROJECT_CORE §5.5）。
- 汇率不硬编码：引入唯一的第三方数据接口 Frankfurter（ECB 数据），失败即降级，绝不阻塞费用展示；来源与数据日期直接标注在换算行内。
- 日本 eVISA 系统内实际扣款金额仍属未核验遗留项（DECISION.md §5-6），故金额标「约」并在 note 与免责句双重指向支付页为准。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过（曾因 dev server 占用 `.next` 构建失败一次，停服后复跑通过）。
- 375×812 走查：/start 三卡显示「现在开始，预计 2026 年 8 月 10/12 日前拿到（估算）」且换行不拆词；CA 面板 CAD 100+85=185 ≈ USD 131（1 CAD ≈ 0.7058，ECB 2026-07-09）；JP 面板「约 JPY 15,000 ≈ USD 92」；KR 面板 USD 40 无换算行；修复「总计」两字被挤竖排；无横向溢出；控制台零错误零警告。

## 2026-07-09 12:01:51 PDT

### 修改范围（视觉系统「路径感 · Wayfinding Calm」落地）

- 依据 Claude Design 交接包（`签证助手 视觉系统.dc.html`）实现全站视觉系统，无新依赖：
- **Tokens**：`tailwind.config.ts` 定义全站唯一强调色深杉绿 `pine #16594B`（hover `#0E4237` / tint `#E7F0EC`）、纸面 `#F6F5F1`、三级描边（`E3E2DA/EDECE5/F2F1EA`）、四级文字（`1D2521/566059/7E8781/A6ADA7`）、死线琥珀（`FAF3DF/E9DCB4/7A5B12`）与清除红（`8C3A2E`）。规则：绿只给「操作与进度」、琥珀只给「有期限的事实」、红仅「清除数据」一处；无渐变、无第二强调色、阴影仅 `0 1px 2px / 4%`、1px 描边分层。`globals.css` 换 Noto Sans SC 优先的 CJK 栈、去径向渐变背景；移除 Geist 本地字体（`app/fonts` 删除）。
- **信任小组件**（新增单文件组件）：`BrandMark` 品牌路径标、`VerifiedChip` 核验 chip（等宽日期）、`DeadlineChip` 琥珀死线 chip（陈述期限、不倒数）、`BoundaryNote` 一行边界声明、`PrivacyClearFooter` 隐私声明 + 一键清除页脚。
- **里程碑进度条重做**（`FlowProgress`，视觉签名）：完成 = 26px 实心杉绿 ✓、当前 = 30px + 4px 浅绿环 + 标签加粗、未来 = 空心数字；2px 连接线；死线节点挂琥珀 chip；已完成节点可点回看、未来节点不可跳；移动端 92px 节点横向滚动 + 当前节点自动居中；条下一行「第 N / M 步 · 全程平均约 X，按提交日估算」。全程完成度百分比条按设计移除，任务完成度改为当前步卡「已完成 n / m」（不适用项剔除分母）。
- **死线结构化**：`FlowStep.deadline`（chip / note / eventId / days）；CA `biometrics` = 「限 30 天」（BIL 收信日 + 30 天可推算到期日，已核验值）、CA `decision` = 「限期寄达」（PPR 截止天数未经核验，不硬编码数字）。`lib/domain/eta.ts` 新增纯函数 `averageEtaLabel`（阶段平均天数 → 约 N 天/周/个月）与 `deadlineDateFrom`；`lib/flows.ts` 新增 `getOrganizationShortName`（IRCC 类缩写提取，VFS 类取括号前缀）。
- **Checklist 四态行**（`ChecklistItemCard`）：行式四态——待办（空框 + 「跳过 / 不适用」文字操作）、完成（实心杉绿 ✓ 不划线）、跳过（虚线框 – + 「已跳过」pill）、不适用（灰底 ✕ + pill、剔除分母）；pill 可点恢复待办；必做项规则不变（仅 `allowNotApplicable` 可标不适用），非必做项显示建议/视情况/阅读确认小 pill。
- **屏 1 入口重构**：`/start` 一屏两段——① 身份卡（F-1 / H-1B 点选即存 + 回读校验，未开放身份收敛为一行灰字）② 国家卡（共 N 步 + 平均 ETA + 核验信息；有真实进度的卡带常驻续接条「上次进行到第 N 步 + 继续 →」）；首访仍先过使用须知必读门（适用条件三条并入须知、单勾选同意）；顶部一行边界声明常驻。`/countries` 并入 `/start` 并重定向；`/ca` `/jp` `/kr` 旧重定向改指 `/start`；sitemap 移除 `/countries`。
- **屏 2 流程页重构**（`FlowRunner`）：页面级页头（移动 = 返回 + 国旗标题 + 身份 chip；桌面 = 品牌 / 面包屑 / 「适用身份」chip）→ 全宽白底里程碑条 → 当前步卡（「当前 · 第 N 步」眉标、Checklist、每屏唯一实心主按钮 = 「打开 {机构} 官方入口 ↗」、边界声明 + 核验 chip）→ 其余节点卡（完成 ✓ + 回看链接、未来编号 + 死线注记）→ 描边式上一步 / 下一步。桌面 1fr + 320px 右栏：本步来源（逐条官方链接 + 各自核验日期）、后程期限（录入 BIL 收信日期后显示具体到期日）、隐私 + 清除。`FlowOverviewPanel` 与 `EtaEstimator` 改为当前步卡内嵌节；`OfficialLinkCard` 删除（来源改右栏列表）。
- **全局 chrome**：根布局不再挂 SiteHeader / SiteFooter（入口页与流程页自带页面级 chrome），首页 / 关于 / 隐私 / 404 显式引入重绘后的页头页脚；首页去渐变、单实心 CTA，国家卡展示配置驱动的步数与平均 ETA。

### 关键决定

- 设计稿屏 1 未画使用须知门；为守住 0.2 起的「使用须知必读门」产品底线，保留首访门（适用条件并入门内、单勾选），之后信任信号按设计转为「小而常驻」（通告条 / 核验 chip / 边界声明 / 页脚清除）。
- 设计稿节点 6 死线标「限 30 天」；PPR 递照截止天数本站从未核验，按内容规则不硬编码数字——chip 用「限期寄达」、正文写「以通知标注的截止日期为准」；BIL 30 天为已核验值，保留并支持到期日推算。
- 未来节点不可点击跳转（设计定义「已完成节点可点回看」），前进只走「下一步」；每屏唯一实心主按钮（入口 = 继续 / 同意，流程页 = 官方入口 CTA），步骤导航降级为描边按钮。
- 「平均约 1 个月」类 ETA 标签不硬编码，由各流程 `etaStages` 平均天数换算（<10 天 / <28 周 / 其余按月），CA 得「约 1 个月」、日韩得「约 3 周」。
- 国旗 emoji 在 Windows 预览环境降级显示为字母国家码，属平台字体现象，真机正常，不改实现。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过；静态页仍为 56（6 流程 × 7 步 + 静态页）。
- 375×812 走查（dev server）：须知门 → 同意 → F-1 点选 → 三国卡（CA「平均约 1 个月」、JP/KR「约 3 周」，链接正确）→ CA 流程页（里程碑条 + 死线 chip、当前步卡、费用总览与 ETA 嵌节、「打开 IRCC 官方入口 ↗」、四态交互：勾选 1/2 → 不适用 1/1 → 恢复 1/2、其余节点死线注记）→ 第 5 步录入 BIL 日期 2026-07-09 → 「后程期限」显示到期日 2026-08-08、进度条自动居中（scrollLeft 234.5）→ 回 /start 见续接条（第 5 步 + 继续 → 指向断点）。控制台无错误、无警告。
- 修复走查发现的横向溢出：进度条内 sr-only（absolute 定位）读屏文本无定位祖先、逃出滚动容器把 html 撑到 606px；轨道加 `relative` 后 375 = 375。
- 桌面 1280 走查：品牌 / 面包屑页头、flex-1 节点条、1fr + 320px 右栏（来源 / 后程期限 / 隐私卡）正常。
- 本机无 Node：上次会话的便携版已不在，重新下载便携 Node v20.19.0 至 `D:\n` 短路径，`npm ci` 后完成全部验证。

## 2026-07-08 23:08:14 PDT

### 修改范围

- `docs/BUSINESS_PLAN.md` 刷新至 2.0（基线 0.3）：终局蓝图改写为「用户共建的签证互助社区」三层模型（工具层→数据层→社区层）；痛点章节围绕小红书结构性缺陷（时效不可判断/无陪伴感/碎片化启动成本）重写并附证据；目标用户明确为「18–28 岁在美无绿卡中国留学生」滩头 + 三级扩展路径（绿卡人群→非中国籍留学生→各国互签矩阵）；新增可比产品生死教训表（levels.fyi/NomadList/VisaJourney/Trackitt/穷游/马蜂窝）与四条共建铁律；商业模式按可比产品验证路径重排（C 轻变现→A 数据资产→B 机构）；路线图重排为上线验证→数据层 MVP→账号与社区→扩展复盘。

### 关键决定

- 市场调研经 12 智能体（6 路检索 + 6 路对抗核验）完成，全部硬数字附来源 URL 与口径；核验环节修正多处错误后才写入：IRCC 开放数据被调研员双重计数（真实值为全球访客签批准 2024 年 145.8 万、中国籍 25.4 万，非翻倍值）、韩国恢复率 60% 系单月口径（全年约 76%）、Similarweb 旧快照（一亩三分地现约 220 万月访问）、NomadList 现行定价（Lite $19.99/Full $119，非 $99）等。
- 自称/未审计数字（VisaJourney 10 万申报者、Lawfully 250 万用户等）一律标注「自称、未经核实」，不作决策依据；市场规模弃用口径混乱的行业报告，采用自下而上估算 + VFS Global（2023 年 2410 万份申请）作为量级锚点。
- 维持「免费核心、不代办、不碰材料、变现不污染数据可信度」底线；明确不以独角兽叙事包装，目标为 levels.fyi/VisaJourney 量级的可持续垂直产品。

### 验证

- 仅新增/更新 Markdown 文档，未修改源码、配置或依赖，无需运行 TypeScript、lint 或生产构建。
- 全部写入企划书的外部数字经对抗核验员重新打开来源 URL 复核（12 个核验 verdict 中被证伪的数字均已按修正值写入或弃用）。

## 2026-07-08 11:05:49 PDT

### 修改范围（0.3：身份先行 + 三国流程 + 里程碑进度条）

- **身份先行 → 国家选择**：`/start` 只做使用须知 + 全局身份确认（`UserIdentitySelection` 存 `visa-flow:identity` 单一键，与流程解耦）；新增 `/countries` 国家选择页（`CountrySelector`），按身份列出可申请国家卡片并预览全部里程碑节点；`FlowIdentitySelection`（按流程存身份）废弃。
- **新增 H-1B 身份**：`config/identities.ts` 定义全站身份选项与硬性适用条件；各国流程以 builder 参数化身份差异（F-1: I-20/在读证明；H-1B: I-797/在职信/工资单），共 6 条配置（CA/JP/KR × F1/H1B），版本 0.3，旧 0.2 进度因版本与 ID 不符被规范化丢弃（沿用既往先例）。
- **新增日本流程**（`jp-visitor-f1/h1b`，7 步）：主路径 JAPAN eVISA（在美中国公民可用，单次短期观光，电子出签、值机须联网实时展示），领馆纸质申请（多次往返/无信用卡等）作条件分支任务。**日本签证费 2026-07-01 已上调**（美国领区单次 $20→$101、多次 $40→$201，48 年来首次），已按新标准写入并在文案中提示旧攻略过时。
- **新增韩国流程**（`kr-visitor-f1/h1b`，7 步）：C-3-9 经管辖领馆申请（K-ETA 对中国大陆护照不适用、对华团体免签不适用从美出发个人）；「查明管辖领馆现行要求」设为独立步骤（各馆亲递/邮寄、流水口径、money order 抬头差异极大）；过境免签 B-2 作条件分支；无生物采集，`FlowConfig.biometricsFee` 改可选。
- **常识类条件单框化**：「能按时返回」「有旅行计划」「无犯罪记录」等合并为各流程第一步的单个必做勾选（`detailPoints` 展开可见明细），原 eligibility 步骤删除（8→7 步）；资金 CAN+ 展开说明移至「准备材料」资金证明项。
- **CAN+ 措辞修正**（IRCC qnum=877 2026-07-07 实测）：CAN+ 为自动加速审理条款（近 10 年加签或有效美签二选一）；认护照上的签证贴纸——H-1B 贴纸过期仅 I-797/I-94 有效时大概率不享受加速但不影响申请资格；配置文案已按此修正，并补北美票帝加拿大指南为第三方来源。
- **全程里程碑进度条**：`FlowStep.milestone` 新增；`FlowProgress` 重做为可点击节点 stepper（完成/当前/未来三态、自动横向居中当前节点）+ 完成度条；国家卡片同步预览节点链。
- **美术升级**：CJK 字体栈、径向渐变背景、sticky 毛玻璃页头 + 品牌标、渐变主标题/进度条、卡片阴影层次、首页三国卡片 + 四步「怎么用」。
- **清理**：删除空壳攻略系统（`app/jp|kr/[slug]`、`lib/guides.ts`、`lib/types.ts`、`content/guides`、`data/*.json`、旧 `config/flows/ca-trv-f1.ts`）；`/ca`、`/jp`、`/kr` 重定向到 `/countries`；`getDefaultFlow` 移除；sitemap 加 `/countries`；「清除进度」升级为清除本站全部本地数据（含身份）；ETA 组件文案去 CA 化（「收到贴签护照」→「拿到签证」）。
- 根目录新增 `DECISION.md` 记录本轮决策与待确认项；`.eslintrc.json` 加 `root: true`（worktree 嵌套时防止 ESLint 向上爬找主仓库配置）。

### 关键决定

- 第二身份默认 H-1B（备选 OPT 已在 DECISION.md 列出待确认）；身份是全站单一记录，换国家不重复确认。
- 日韩内容来自 6 路多智能体联网调研（官方 MOFA/驻美使领馆/visa.go.kr/K-ETA + 北美票帝 piao.tips/美卡论坛），官方优先、新页面优先；第三方经验一律以「第三方经验/本站建议」定性并列出原文 URL；易变值不硬编码、全部带 `lastVerified: 2026-07-07`。
- 日本官网反爬（403），费用等关键事实经 MOFA 公告 + 底特律领馆费用表 + Wayback 快照多源交叉；遗留人工核验项（eVISA 系统内涨价后实际扣款金额等）记录在 DECISION.md §5-6。
- 里程碑节点 = 流程步骤（每步 2–4 字短标签），第一步即可见全链并可点击跳转。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过；静态页 21 → 56（6 条流程 × 7 步 = 42 个流程页）。
- 375×812 走查（dev server）：须知→身份（F-1/H-1B 可选、4 个未开放禁用）→国家选择（三国卡片 + 节点链 + 「进行中」徽标）→ CA/JP/KR 流程页（7 节点 stepper、身份/国家标签、费用面板日本无生物费栏、ETA 估算 5–32 天、H-1B 材料变体、常识单框展开明细）均正常；无横向溢出；控制台无错误无警告。
- 本机无 Node：使用便携 Node v20.19.0（解压至短路径）完成全部验证。
- 多智能体对抗复审（4 维度 review + 逐条证伪核验，21 个 agent）确认 15 项缺陷并全部修复后复测：多标签页 storage 事件同步（防盲覆盖与「清除后进度复活」的隐私违背）、空进度不落盘 + 国家卡片「进行中」判定收紧、「继续这条流程」恢复到 `currentStepId` 断点、`FlowRunner` 改用 `processingTimeSourceId`（修复日韩丢失官方处理时间入口）、stepper 对比度（slate-400→500）/ 滚动定位（getBoundingClientRect）/ 读屏状态（sr-only）、localStorage 禁用时安全降级 + 身份保存回读校验、`normalizeProgress` 补 timelineDates 清洗、日韩配置 4 处「第三方经验/官方要求」定性与来源匹配修正（含删除未经核实的济州免签条目）、CAN+ 来源 lastVerified 补记 2026-07-07。修复后 tsc / lint / build 复跑通过，国家卡片三态（继续到断点 / 空记录视为开始 / 真实进度显示进行中）走查确认。



### 修改范围（0.2 重构）

- **国籍参数化**：`FlowConfig` 增 `nationality`，不再把「中国护照」写死在 UI / 文案；身份门改由 `eligibilityConditions` 数据驱动。0.1 内容仍只做中国。
- **流程精简 16 → 8 步**：合并为 eligibility / overview / prepare / apply / submit / biometrics / decision / complete；`FlowStep.timelineEvent` 改为 `timelineEvents[]` 承载合并后多个时间节点；说教类 informational 任务移入使用须知门。
- **使用须知必读门**：新增 `UsageAgreement` + `StartFlow`，未勾选同意不能进入；`FlowIdentitySelection.consentAcceptedAt` 记录同意时间，`useFlowProgress` 身份有效性要求已同意。
- **身份两按钮门**：`IdentitySelector` 改为条件只读清单 +「我符合以上全部 / 有不符合的」；不符合可下钻选出具体条件并给官方判断工具出口。
- **拿签 ETA 估算器**：新增 `lib/domain/eta.ts` 纯函数与 `EtaEstimator`，按计划提交日叠加四阶段区间给出拿签日期区间与分段（官方 / 经验标注），带强免责，提交日存 `timelineDates.etaSubmitDate`。
- **资金口径（无官方固定额 + CAN+）**：`FlowTask.detailPoints` 承载多行说明，`ChecklistItemCard` 可展开；资金任务写明自查基准、证明形式、F-1 持有效美签可走 CAN+ 免交（「可不交」≠「不需具备」）、第三方约 3,000 USD 经验值。
- **官方获取入口**：`sources` 新增 I-94（CBP）、IRCC Portal 流程/登录/账户、照片规格、是否需要采集、采集地点、支持文件、CAN+、check-visa-eta、各国籍入境要求、VFS 护照递交、I-20 背景说明（无下载链接，不编造）。
- `FlowConfig.version` → `0.2`；旧 0.1 本地进度因版本不符经规范化丢弃（个人项目可接受）。

### 关键决定

- 所有易变值（费用、处理时间、VFS）不硬编码，指向官方当前页并标注 `lastVerified: 2026-06-25`。
- ETA 周数不写死：四阶段以「最短/最长 + 官方/经验来源」配置，官方处理段强标「以官方查询工具当日为准」，UI 明示估算非保证、官方处理不含生物信息与邮寄。
- 资金不写死金额；CAN+ 精确表述仅限 TRV、不等于不需具备。
- 第三方经验（北美票帝 ~3,000 USD）标为经验、非官方；I-20 无公开下载链接，只提示向学校 DSO 索取。
- 数据来自多智能体核验（IRCC / CBP / VFS 官方页面 + 第三方），实现前已成研究简报。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过；静态页 29 → 21（流程步骤 16 → 8）。
- 375×812 浏览器走查（dev server）：使用须知门、8 步进度、费用面板（CAD 100 / 85、核验 2026-06-25）、ETA 估算（06-25 提交 → 约 07-14 至 08-23、19–59 天、常见 3–6 周、分段官方/经验、免责与官方入口）、资金 CAN+ 展开与三条官方入口，均正常、无横向溢出、控制台无错误。
- 旧 0.1 进度版本不符被规范化丢弃，符合预期。

## 2026-06-25 16:11:20 PDT

### 修改范围

- 新增 `docs/BUSINESS_PLAN.md`，基于 0.1 现有代码、产品原则和流程配置形成完整中文商业企划书。
- 盘点并写明当前真实能力：1 条已开放流程、16 个步骤、83 个任务、9 个官方来源、7 个时间线节点、本地进度及可替换 Repository 架构。
- 补充目标用户、市场切入、竞争差异、商业模式、获客策略、12 个月路线图、核心指标、财务假设、风险控制和未来 90 天执行清单。
- 引用 Open Doors 2025、美国国务院签证统计入口和 IRCC 开放数据入口，市场数字与收入预测分别标注为外部事实和内部验证假设。

### 关键决定

- 保持项目当前“免费、作品集性质、无商业化”的事实不变；企划书被定义为商业化可行性方案，不代表已经收费。
- 推荐“免费核心 + 付费便利功能 + 机构合作”，不以代填、代交、材料托管、保签或出售用户线索作为收入来源。
- 将真实留存、进入官方提交后阶段的流程数和内容核验成本设为商业化前置证据，避免用尚未实现的登录、数据库、其他国家流程或用户规模包装项目。
- 将中国留学生公开数据作为首发人群规模的方向性依据，而非直接等同于持中国护照且需要加拿大签证的可服务市场。

### 验证

- 对照 `AGENTS.md`、`README.md`、`docs/PROJECT_CORE.md`、`docs/PRODUCT_SPEC.md`、`docs/CA_F1_V01_FLOW.md` 与现有流程配置完成事实核对。
- 使用代码检索复核流程统计：16 步、83 个任务（60 必做、13 建议、7 条件、3 阅读确认）、9 个官方来源、7 个时间线节点。
- 本次仅新增和更新 Markdown 文档，未修改源码、配置、依赖或公开页面，无需运行 TypeScript、lint 或生产构建。

## 2026-06-24 19:40:28 PDT

### 修改范围

- 新增 `FlowTask.allowNotApplicable`，允许「满足条件才适用」的必做任务被标记为不适用。
- 完成度分母剔除被标为不适用的必做项；`countMissingRequiredTasks` 同样不再把不适用的必做项计为未完成。
- `ChecklistItemCard` 改为：非必做项显示「暂时跳过 + 不适用」，带 `allowNotApplicable` 的必做项只显示「不适用」，普通必做项不显示。
- 为 `biometrics` 步骤的四个采集类必做项（阅读 BIL、预约、准备现场文件、完成采集）开启 `allowNotApplicable`，并在说明中点明「若官方确认无需采集，可标记不适用」。

### 关键决定

- 采用精准标记而非「所有必做项可标不适用」：只有生物信息采集这类条件适用的必做项可被标不适用，护照、缴费等普适必做项不受影响，避免误标与界面冗余。
- 解决既往生物信息 10 年内有效、无需采集的用户完成度卡在 <100% 的问题（CA_F1_V01_FLOW Step 8「标记不适用并进入下一步」）。

### 验证

- `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通过，29 页、F-1 仍 16 步。

## 2026-06-24 15:12:18 PDT

### 修改范围

- 多智能体审计 0.1 实现对照三份规格，去重后修复优先级最高的 Now 批次。
- 完成度算法改为只统计当前适用的核心（必做）任务，修复「做完全部必做也到不了 100%」的语义错误（PRODUCT_SPEC §7.4）。
- `TaskState` 扩展为 `completed | skipped | not-applicable` 四态，新增 `TASK_STATES` 常量；`ChecklistItemCard` 把「暂时跳过/不适用」拆为两个独立可切换控件，带 `aria-pressed`、≥44px 热区与选中态的非颜色线索（✓+文案+边框，满足 WCAG 1.4.1）。
- `normalizeProgress` 改用 `sanitizeTaskStates` 白名单清洗任务状态，向后兼容旧两态、丢弃脏值。
- 新增 `FlowOverviewPanel`，在 `overview` 步（`FlowStep.showSummaryPanel`）顶部呈现官方申请费、生物信息费、官方处理时间入口与「数据收集中」时间线占位及核验日期；此前 `officialFee`/`biometricsFee` 字段全站零消费。
- `resetProgress` 清除进度时一并清除身份选择键，并失效后由跳转 effect 统一回到 `/start`，兑现隐私承诺。
- `IdentitySelector` 未开放身份提示块新增官方访客签证入口，消除死胡同。
- `FlowProgress` 增加「完成度仅统计必做项」说明，降低勾选建议项却见进度不动的认知落差。

### 关键决定

- 完成度分母 = 全部 `required` 任务，分子 = 其中 `completed`，确保 100% 可达且符合「本站流程完成度」语义。
- 0.1 仍只允许在非必做任务上标「不适用」；`required` 任务暂不可标不适用（生物信息无需采集等场景的处理留作产品决策与条件分支引擎）。
- 改动经 3 视角对抗式复审（领域/状态副作用/无障碍），裁决 fix-then-ship：确认无清除-回写竞态、无除零、空值守卫到位，仅修一处颜色单一线索的无障碍缺陷。

### 验证

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过，无警告。
- `npm run build`：通过，共 29 页；F-1 流程仍静态生成 16 步，流程页因新面板与四态控件体积略增。

## 2026-06-24 14:05:47 PDT

### 修改范围

- 核验加拿大访客签证 F-1 流程的官方易变信息，并把结论写入 `config/flows/ca-trv-f1.ts` 与 `docs/CA_F1_V01_FLOW.md`。
- 为 16 步流程的全部任务补全 `description`，统一加「官方要求 / 本站建议 / 视情况 / 准备项 / 官方填写项 / 进度确认」定性前缀，并说明用途与常见遗漏。
- 在 `sources` 新增 IRCC 费用清单来源 `fees`；更新已核验来源（`eligibility`、`biometrics`、新 `fees`）的 `lastVerified` 为 2026-06-24，顶层 `lastVerified` 同步更新。
- 更新 `officialFee` / `biometricsFee` 字段措辞，补充家庭上限并标注以官方页面为准。
- 新增 `getTaskSources` 解析任务级官方来源；`ChecklistItemCard` 在说明下方渲染逐项官方来源链接（机构 + 标签，安全新标签页打开），`FlowRunner` 负责注入。
- 为关键任务（资格、材料、账户、生物信息、护照递交等）填写 `sourceIds`。

### 关键决定

- 已核验官方值：访客签证申请费每人 CAD 100（家庭 5 人及以上最高 CAD 500）；生物信息费每人 CAD 85（家庭 2 人及以上最高 CAD 170）；14–79 岁需采集且提交时缴费；BIL 自标注日期起 30 天内采集、可申请延期；既往生物信息 10 年有效；生物信息耗时不计入处理时长。来源为 IRCC 官方页面。
- 易变费用数字不写入任务文案，只存于 `config` 字段并标注核验日期，留待总览页统一呈现，避免内容层硬编码。
- I-20、I-94、在读证明定性为支持性「本站建议」材料；护照、资金证明等定性为「官方要求」；最终一律以 IRCC Portal 个性化清单为准。
- VFS 美国站护照递交的地址、服务费、付款与回邮方式不硬编码，文案统一指向 VFS 当前页面。
- 第三方参考（北美票帝）仅用于操作顺序与常见坑，以「常见经验」措辞写入，不作为官方要求依据；原文 URL 保留在 `docs/CA_F1_V01_FLOW.md` §9。

### 验证

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过，无警告。
- `npm run build`：通过，共生成 29 个页面；F-1 通用动态路由仍静态生成 16 个步骤。
- 类型与渲染：`FlowTask.sourceIds` 经 `getTaskSources` 解析后在 Checklist 卡片正常渲染，无任务时不渲染来源区块。

## 2026-06-24 00:11:06 PDT

### 修改范围

- 将流程进度创建、规范化、任务更新、日期更新、完成度计算和必做项判断拆分到 `lib/domain/progress.ts`。
- 新增 `FlowProgressRepository` 与 `FlowIdentityRepository` 存储契约。
- 新增 localStorage Repository；React 组件不再直接读写 localStorage。
- 新增 `useFlowProgress`，统一协调身份校验、进度加载、自动保存和清除操作。
- 为 FlowConfig 增加国家、签证类型和身份 slug。
- 将固定 F-1 页面改为 `/apply/[country]/[visaType]/[status]/[step]` 通用动态路由。
- 扩展流程注册表，统一负责配置查找、路径生成和静态参数生成。
- 新增未来数据库使用的 `ApplicationRun`、任务状态和时间线记录契约，但 0.1 不进行远程持久化。
- 新增 `docs/PROJECT_CORE.md`，作为项目长期产品、隐私、内容与架构指导。
- 重写 README，完整说明项目意义、目标人群、核心价值、当前能力和未来登录/数据库方向。
- 更新 `AGENTS.md`，要求后续工作优先阅读项目核心指导。

### 关键决定

- 项目继续采用 Next.js 模块化单体，不提前拆分独立 Express 后端。
- UI、领域逻辑、流程配置和存储实现保持单向依赖。
- 未来数据库上线时新增远程 Repository 与服务端 DAL，不重写主要流程组件。
- 通用页面只通过流程注册表解析配置，不认识具体国家或身份规则。
- 进度增加 `currentStepId`、`revision`、`createdAt` 和 `updatedAt`，为登录后同步与冲突处理预留字段。
- 未来登录用户可以认领本地匿名进度；敏感签证内容仍不进入数据库。

### 验证

- `npm run lint`：通过，无警告。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过，共生成 29 个页面。
- 通用动态流程路由静态生成 16 个加拿大 F-1 步骤。
- 375×812 回归：身份选择、配置路由跳转、下一步导航和无横向溢出通过。
- 旧版 0.1 localStorage 进度可通过规范化逻辑继续读取。
- 浏览器控制台错误与警告：无。

## 2026-06-23 22:20:18 PDT

### 修改范围

- 新增配置驱动的流程类型、读取逻辑和 `ca-trv-f1-adult` 0.1 配置。
- 新增身份选择入口，只允许已开放的 F-1 配置进入流程。
- 新增通用流程渲染器、Checklist、官方来源卡片、流程进度和本地日期节点。
- 建立 16 步加拿大 F-1 访客签证流程，从身份确认持续到收到护照和整理匿名时间线。
- 新增站点页头、页脚、隐私页、404、robots 和 sitemap。
- 重写首页、关于页和 README，使项目定位从文章攻略站切换为申请步骤助手。
- 将旧加拿大攻略路由导向身份选择页。
- 更新美国身份类型和状态数据，移除当前不覆盖的绿卡路径。

### 关键决定

- 页面组件不写死 F-1 规则；身份和签证差异由 `config/flows` 配置提供。
- Checklist、所选身份和流程日期仅保存到当前浏览器的 localStorage。
- 0.1 不实现跨用户时间线收集，只为未来匿名聚合统计保留日期节点。
- 费用、VAC 要求和官方入口均显示核验日期，易变信息不写入页面逻辑。
- 用户必须先完成身份选择；直接进入流程页会返回 `/start`。

### 验证

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过，无警告。
- `npm run build`：通过，共生成 29 个页面，其中 F-1 流程静态生成 16 个步骤。
- 375×812 浏览器验收：首页和流程页无横向溢出。
- 身份选择、进入流程、Checklist 勾选和刷新后恢复进度：通过。
- 浏览器控制台错误与警告：无。

## 2026-06-23 22:11:11 PDT

### 修改范围

- 建立统一的项目修改日志。
- 将 `CLAUDE.md` 改为指向 `AGENTS.md`，避免多份规则漂移。
- 更新 `AGENTS.md`，要求所有后续 AI 在修改源码前阅读本文件。
- 开始将项目从文章攻略骨架重构为配置驱动的签证申请流程助手。

### 关键决定

- 0.1 版本只支持中国护照、年满 18 岁、美国 F-1 在读学生申请加拿大访客签证。
- 用户先确认身份，系统再加载对应 config。
- 流程范围从申请前准备持续到收到并核对贴签护照。
- 不升级依赖，不引入数据库，不收集敏感申请信息。

### 验证

- 重构前运行 `npm run build`：通过。
